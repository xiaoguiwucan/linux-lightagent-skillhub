# 审核规范

维护者在合并前确认：用途和触发条件清晰；元数据完整；依赖、域名、文件和工具声明与实现一致；无密钥、隐藏遥测、混淆代码或来源不明素材；敏感操作有必要性且已明确说明；版本和兼容范围合理；评测覆盖主要成功与失败路径。

自动检查通过不代表技能安全。涉及命令执行、下载后执行、凭据处理、外部上传或工作区外写入的技能至少需要一名 CODEOWNER 明确批准。

审核 Schema v2 脚本技能时，还要确认每个脚本入口都有结构化声明，参数约束与实现一致，正文没有直接 shell 执行路径，系统组件使用稳定能力名而不是包管理命令。

涉及微信群时，审核者还要确认 stable room/member 授权、普通成员工具过滤、workspace 媒体路径和真实发送顺序。声明 `ordered-text-attachments` 的技能必须有测试证明 `reply_text` 稳定、附件路径受限、`delivery_order` 固定为文字后附件；不得只靠模型提示词保证关键格式。
